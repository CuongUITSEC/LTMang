<UserControl x:Class="Learnify.Views.PomodoroView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Learnify.Views"
             xmlns:vm="clr-namespace:Learnify.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800">


        <Grid Background="#DFF3FF">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">

                <!-- Vòng tròn và thời gian -->
                <Grid Width="200" Height="200">
                    <Ellipse Fill="WhiteSmoke"/>

                    <!-- Vòng Pomodoro (màu đỏ) -->
                    <Path Data="{Binding PomodoroArc}" Fill="#FF4F5B" Opacity="0.8"/>

                    <!-- Vòng Break (màu xanh) -->
                    <Path Data="{Binding BreakArc}" Fill="#4F93FF" Opacity="0.8"/>

                    <!-- Hiển thị thời gian -->
                    <TextBlock Text="{Binding TimeDisplay}" FontSize="24" FontWeight="Bold"
                           VerticalAlignment="Center" HorizontalAlignment="Center"/>
                </Grid>

                <!-- Các nút điều khiển -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
                    <Button Content="BẮT ĐẦU" Command="{Binding StartCommand}" 
                        Background="#FF4F5B" Foreground="White" Width="100" Height="40"
                        FontWeight="Bold" Margin="10" />
                    <Button Content="TẠM DỪNG" Command="{Binding PauseCommand}" 
                        Background="#94D9F8" Foreground="White" Width="100" Height="40"
                        FontWeight="Bold" Margin="10" />
                </StackPanel>

            </StackPanel>
        </Grid>
    </UserControl>
